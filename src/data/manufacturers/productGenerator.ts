
import { AffiliateProduct } from '@/types/affiliate';

/**
 * Product generator has been disabled to prevent sample data creation
 * All products should come from real data sources or user input
 */
export const generateManufacturerProducts = (manufacturerId: string, count: number = 8): AffiliateProduct[] => {
  console.warn('Sample product generation has been disabled. Please add real products through the application interface.');
  return [];
};
