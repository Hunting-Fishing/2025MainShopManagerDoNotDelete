
// All sample data seeding has been removed to ensure real data usage only
// This file is kept for future real data seeding needs if required

console.log('Sample data seeding has been disabled. All service data now comes from the database.');
console.log('Use the service management interface to add real service categories, subcategories, and jobs.');

export default function seedDatabase() {
  console.log('All sample data seeding has been disabled. Use the application interface to add real service data.');
}
